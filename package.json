{
  "name": "habit-builder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.0.0",
    "svelte": "^5.28.1",
    "vite": "^6.3.5"
  },
  "lint-staged": {
    "src/**/*.{js,svelte,css}": [
      "npm run lint",
      "npm run format -- --write",
      "npm test -- --bail",
      "git add"
    ]
  }
}
